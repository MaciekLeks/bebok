const mockdevices = @import("../mocks/devices.zig");
const testing = @import("std").testing;

// test "test" {
//     const md1 = mockdevices.MockBlockDevice;
//     const md2 = mockdevices.MockBlockDevice;
//     try testing.expectEqual(md1.kind, md2.kind);
// }
